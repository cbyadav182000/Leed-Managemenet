version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: leadflow-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=leadflow

volumes:
  mongo-data:
